using UnityEngine;

[CreateAssetMenu(menuName = "Level/Events/SpawnItem")]
public class SpawnItemEvent : TimelineEvent
{
    // Item item;
    public override void Execute(GameController gc)
    {
        // gc.SpawnItem(SpawnInterval, item);
    }
}